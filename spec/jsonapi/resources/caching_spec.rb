require 'spec_helper'

describe Jsonapi::Resources::Caching do
  it 'has a version number' do
    expect(Jsonapi::Resources::Caching::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
